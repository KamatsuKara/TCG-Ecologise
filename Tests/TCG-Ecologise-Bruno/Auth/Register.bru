meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "name": "admin",
    "email": "admin@example.com",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
