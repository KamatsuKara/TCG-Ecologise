meta {
  name: Refresh
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/refresh
  body: json
  auth: none
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", res.body.jwt);
  }
}
