meta {
  name: Create User
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "John Doe",
    "email": "john.doe@example.com",
    "password": "securePassword123",
    "role": "USER"
  }
}