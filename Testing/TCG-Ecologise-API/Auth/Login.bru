meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login?email=admin@example.com&password=admin123
  body: none
  auth: none
}

query {
  email: admin@example.com
  password: admin123
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", res.body.token);
  }
}